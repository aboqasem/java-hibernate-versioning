spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/versioning
    username: postgres
    password: postgres

  jpa:
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
